package shark
/*
import org.apache.hadoop.hive.ql.session.SessionState
import org.apache.hadoop.hive.cli.CliSessionState
import org.apache.hadoop.hive.conf.HiveConf
import org.apache.hadoop.hive.ql.processors.CommandProcessorFactory
import org.apache.hadoop.hive.cli.CliDriver


object Main {
  private var _driver: SharkDriver = null	
  def main(args: Array[String]){
    val conf:HiveConf = new HiveConf(classOf[SessionState])
    val ss:CliSessionState = new CliSessionState(conf)

    SharkCliDriver.main(args)


    SessionState.start(ss)
    _driver = new SharkDriver(conf)
    var cmd = "select * from u_data"
//    _driver.run(cmd)

  }

}
*/
